import sorting.InsertionSort;
import sorting.SelectionSort;
import util.Util;

public class Test {
    public static void main(String[] args) {
        Integer[] arr = {3, 2, 4, 10, 5, -1};
//        InsertionSort.sort(arr);
        Util.arrayPrint(arr);
    }
}
